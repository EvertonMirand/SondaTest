import styled from 'styled-components/native';
import { ERROR_TEXT } from '../../utils/Colors';

export const Container = styled.View``;

export const ErrorText = styled.Text`
  color: ${ERROR_TEXT};
  margin-top: 5px;
`;
